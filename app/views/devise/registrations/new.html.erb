<h2 class="login-heading">Inscription</h2>
<div class="login-container" data-controller="autocomplete">
  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), data: { turbo: :false }) do |f| %>
    <%= f.error_notification %>

    <div class="row">
      <!-- Left Column for email and password -->
      <div class="col-md-6">
        <div class="card">
          <div class="card-body">
            <div class="form-inputs">
              <%= f.input :email,
                          required: true,
                          autofocus: true,
                          input_html: { autocomplete: "email" },
                          label: 'Email',
                          label_html: { class: "form-label" },
                          wrapper_html: { class: "form-field" } %>
              <%= f.input :password,
                          required: true,
                          hint: ("Minimum #{@minimum_password_length} caractères" if @minimum_password_length),
                          input_html: { autocomplete: "new-password" },
                          label: 'Mot de passe',
                          label_html: { class: "form-label" },
                          wrapper_html: { class: "form-field" } %>
              <%= f.input :password_confirmation,
                          required: true,
                          input_html: { autocomplete: "new-password" },
                          label: 'Confirmation du mot de passe',
                          label_html: { class: "form-label" },
                          wrapper_html: { class: "form-field" } %>
            </div>
          </div>
        </div>
      </div>

      <!-- Right Column for the rest of the information -->
      <div class="col-md-6">
        <div class="card">
          <div class="card-body">
            <div class="form-inputs">
              <%= f.input :nickname,
                          required: true,
                          label: 'Pseudo',
                          label_html: { class: "form-label" },
                          wrapper_html: { class: "form-field" } %>
              <%= f.input :first_name,
                          required: true,
                          label: 'Prénom',
                          label_html: { class: "form-label" },
                          wrapper_html: { class: "form-field" } %>
              <%= f.input :last_name,
                          required: true,
                          label: 'Nom',
                          label_html: { class: "form-label" },
                          wrapper_html: { class: "form-field" } %>
              <%= f.input :address,
                          required: true,
                          input_html: { id: "autocomplete_address", data: { autocomplete_target: "address" } },
                          label: 'Adresse',
                          label_html: { class: "form-label" },
                          wrapper_html: { class: "form-field" } %>

              <%= f.input :latitude, as: :hidden, input_html: { id: "input_latitude", data: { autocomplete_target: "latitude" } } %>
              <%= f.input :longitude, as: :hidden, input_html: { id: "input_longitude", data: { autocomplete_target: "longitude" } } %>
              <%= f.input :postal_code,
                          label: 'Code postal',
                          input_html: { id: "input_postal_code", readonly: true, data: { autocomplete_target: "postalCode" } } %>
              <%= f.input :city,
                          label: 'Ville',
                          input_html: { id: "input_city", readonly: true, data: { autocomplete_target: "city" } } %>
              <%= f.input :date_birth,
                          required: true,
                          as: :date,
                          start_year: Date.today.year - 12,  # Changement de cette ligne
                          end_year: Date.today.year - 100,  # et de celle-ci
                          label: 'Date de naissance',
                          label_html: { class: "form-label" },
                          wrapper_html: { class: "form-field" } %>

            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "S'inscrire", class: "btn btn-primary" %> <br>
      <br>
      <p>Tu as déjà un compte ? <%= link_to 'Se connecter', new_user_session_path, class:"btn btn-primary" %></p>

    </div>
  <% end %>
</div>
