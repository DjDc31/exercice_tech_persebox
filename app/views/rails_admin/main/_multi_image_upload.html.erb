<% if field.value.present? %>
<% field.value.each do |image| %>
  <%= image_tag main_app.rails_blob_path(image), class: 'admin-edit-pic', style: 'max-width: 100px; max-height: 100px;' %>
  <%# <%= check_box_tag("#{field.name}_remove[#{image.id}]", 'true', false) %>
  <%# <%= label_tag("#{field.name}_remove[#{image.id}]", 'Remove image') %>
<% end %>

<% end %>



<%= form.file_field field.name, multiple: true %>
